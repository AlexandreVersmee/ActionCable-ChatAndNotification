<div class="row">
  <div class="large-12 ">
    <h1>Notifications</h1>
  </div>
</div>

<div class="row">
  <div class="col-lg-12 col-xs-12">
    <p class="text-right"><a>Marquer comme lu</a></p>

    <%= will_paginate @notifs %>

    <div id="notification"></div>

    <% @notifs.each do |notif| %>
      <%= render partial: "notification", locals: {notif: notif} %>
    <% end %>

    <%= will_paginate @notifs %>
  </div>
</div>